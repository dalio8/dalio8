# dalio8

This repo contains a minimal React application bootstrapped with [Vite](https://vitejs.dev/).

## Setup

1. Install dependencies:
   ```sh
   npm install
   ```
2. Start the development server:
   ```sh
   npm run dev
   ```

The app entry point is `src/main.jsx` and it renders `App` into `index.html`.
